/**
 * Created by Davide on 16/01/2017.
 */
public class AddCommand extends Command {
    public AddCommand(Player player) {

    }

    @Override
    String execute() {
        //GAME??? (game.players) nella super classe!?

        //player

        //player già presente?

        //players.add(player)

        //----------------


        /*
        *  Player player = new Player(commandParser.getPlayerName());
                if (players.contains(player)) {
                    this.state = player.getName() + ": giocatore già presente";
                } else {
                    players.add(player);
                    this.state = "Giocatori:" + printPlayers();
                }
        * */
        return null;
    }
}
